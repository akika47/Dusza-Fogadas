Table games { 
  id integer pk 
  name varchar
  organizerName varchar 

}


Table bets { 
  id integer pk 
  eventId integer
  userId integer
  participantId integer
  prediction varchar 
  betAmount integer
}
/*
Table bet_details { 
  betId integer pk 
  betAmount integer 
}

Table bet_participants { 
  betId integer pk 
  userId integer 
  amount integer 
}
*/
Table results { 
  id integer pk 

  eventId integer 
  result varchar
  participantId integer
}

Table participants {
  id integer pk
  name varchar
  gameId integer
}
Table multiplier {
  id integer pk
  amount double
  eventId integer
  participantId integer
}
Table events { 
  id integer pk 
  eventName varchar 
  gameId integer
}

Table users { 
  id integer pk 
  name varchar
  password varchar
  points integer
  role integer
}
ref: bets.userId > users.id
ref: results.participantId > participants.id
ref: bets.participantId > participants.id
ref: multiplier.eventId > events.id
ref: multiplier.participantId > participants.id
Ref: participants.gameId > games.id
Ref: events.gameId > games.id
Ref: bets.eventId > events.id 
Ref: results.eventId > events.id 
